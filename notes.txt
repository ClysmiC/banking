assumes both computers have same size of unsigned ints, etc.
uses md5 from rosetta
copies skeleton of much of the code from D and C

in memory, all user names are all lowercase. however, from client, usernamesare case insensitive